package me.joseph.cubelets.config;

import java.io.File;

/** Archivo messages.yml del plugin. */
public class MessagesFile extends YamlFile {
    public MessagesFile(File file) {
        super(file);
    }
}

